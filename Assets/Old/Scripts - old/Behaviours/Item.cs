using UnityEngine;
/*
public class Item : MonoBehaviour
{
	[SerializeField] int changedValue;
	[SerializeField] Hero.HeroParamsName changedParam;

	void OnMouseDown()
	{
        GameManager.Hero[changedParam] += changedValue;
        //gameObject.SetActive(false);
        Destroy(gameObject);
    }
}
*/