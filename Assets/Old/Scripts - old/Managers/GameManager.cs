using UnityEngine;
/*
public class GameManager : MonoBehaviour
{
	static Field field;
    static CameraMovement cm;
    
	static int currentLevel = 0;

    public static Hero Hero;
    
	//static List<IInteractable> Interactables;// {} //? List<class>?
    
    public static void NextRandomLevel()
	{
		currentLevel++;
        cm.MoveField();//StartCoroutine(
            //cm.MoveRandomField(field, currentLevel / 5 + (currentLevel - 1) / 5));
  	}

	public static void EndGame()
	{
		//
	}

	void Awake()
	{
		field = GameObject.Find("Field").GetComponent<Field>();
        cm = Camera.main.gameObject.GetComponent<CameraMovement>();
        Hero = gameObject.GetComponent<Hero>();
	}

	void Start()
	{
		NextRandomLevel();
	}
}
*/